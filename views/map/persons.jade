extends ../layout

block content
  style.
    #map {
      width: 100%;
      height: 700px;
    }

  .page-header
    h2
      i.fa.fa-google
      | My Life Map

  #map

  script.
    var lifeMarkers = '!{JSON.stringify(lifeMarkers)}';

  script(src='/js/googleMaps.js')

  script(src='https://maps.googleapis.com/maps/api/js?key=' + process.env.GOOGLE_MAPS_API_KEY + '&callback=initMap')
